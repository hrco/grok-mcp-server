services:
  xbridge-mcp:
    build: .
    image: hrco/xbridge-mcp:latest
    container_name: xbridge-mcp
    environment:
      - XAI_API_KEY=${XAI_API_KEY}
    restart: unless-stopped
    # MCP stdio mode
    stdin_open: true
    tty: true
